# Telestrator MVP

MVP mínimo: Editor + Viewer. El viewer puede añadirse a OBS como Browser Source apuntando a `http://<tu-host>:3000/viewer.html`.

## Requisitos
- Node.js 14+ (preferible 16+)

## Instalación
```bash
git clone <repo> telestrator-mvp
cd telestrator-mvp
npm install
npm start
